const apiUrl = import.meta.env.VITE_API_URL || '';
axios.get(`${apiUrl}/api/events/`)  .then(response => {
    // Handle the response data
    console.log(response.data);
  })
  .catch(error => {
    // Handle any errors
    console.error('Error fetching events:', error);
  });